#include <stdio.h>
#include <stdlib.h>
#include <time.h>    // time()

int main() {
                srand(time(NULL));
    int i = 0;
        for (i = 0; i < 50; i++) {
                        printf("%d\n", rand());
                            }
                                return 0;
                                }
